<h1>Articles</h1>

Our blog has <%= Article.public_count("public") %> articles and counting!

<ul>
    <% @articles.each do |article| %>
    <% if !article.archived? && article.is_deleted == false %>
    <li>
        <%= article.is_deleted %>
        <%= link_to article.id, article %>
    </li>
    <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>